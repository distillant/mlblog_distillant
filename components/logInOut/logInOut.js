/**
 * Created by patrick conroy on 2/5/18.
 */
const loginOut () => (
    <div class="log-in-out">
        (props.user) && props.user.userName) ?
        (<a>Login</a>) :
        (<a>Logout </a>)
        <style jsx>
            .button
        </style>
    </div>

)